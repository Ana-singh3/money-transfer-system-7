# Copy to: config/env.properties
#
# This file is loaded by Spring Boot via `spring.config.import` in application.yml files.
# Keep the real `config/env.properties` OUT of git (it is gitignored).

DB_HOST=localhost
DB_PORT=3306
DB_NAME=moneytransfer
DB_USERNAME=root
DB_PASSWORD=change-me

# Optional overrides for tests
DB_TEST_NAME=moneytransfer_test
DB_TEST_USERNAME=root
DB_TEST_PASSWORD=change-me


